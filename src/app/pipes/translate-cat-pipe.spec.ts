import { TranslateCatPipe } from './translate-cat-pipe';

describe('TranslateCatPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateCatPipe();
    expect(pipe).toBeTruthy();
  });
});
